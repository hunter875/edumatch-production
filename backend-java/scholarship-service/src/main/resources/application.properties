spring.application.name=scholarship-service
server.port=8082

# --- C?u h�nh CSDL (Scholarship) ---
# Local: jdbc:mysql://localhost:3306/scholarship_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:true}
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=${SPRING_FLYWAY_BASELINE_ON_MIGRATE:true}
spring.flyway.baseline-version=1

# --- C?u h�nh JWT (SECRET KEY M?I) ---
app.jwt.secret=${JWT_SECRET}
app.jwt.header=Authorization
app.jwt.prefix=Bearer

# --- C?u h�nh RabbitMQ (Docker: dùng container name) ---
# Local: localhost | Docker: rabbitmq-test | Azure: Service Bus connection
spring.rabbitmq.host=${RABBITMQ_HOST:rabbitmq-test}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME}
spring.rabbitmq.password=${RABBITMQ_PASSWORD}

# --- ??a ch? Auth-Service & Matching-Service ---
# Docker local: http://auth-service-test:8081 | Azure: http://matching-service-app:8000
app.services.auth-service.url=${AUTH_SERVICE_URL:http://auth-service-test:8081}
app.services.matching-service.url=${MATCHING_SERVICE_URL:http://matching-service:8000}

# --- Logging ---
logging.level.org.springframework.security=${LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY:INFO}
logging.level.com.edumatch.scholarship=${LOGGING_LEVEL_COM_EDUMATCH_SCHOLARSHIP:INFO}

# --- Observability ---
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=${MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS:when-authorized}
management.metrics.tags.application=${spring.application.name}
management.metrics.tags.service=scholarship-service
management.metrics.tags.environment=${DEPLOY_ENVIRONMENT:local}
management.metrics.tags.version=${APP_VERSION:local}
management.metrics.tags.commit=${GIT_COMMIT:local}
management.info.env.enabled=true
info.app.name=${spring.application.name}
info.app.environment=${DEPLOY_ENVIRONMENT:local}
info.app.version=${APP_VERSION:local}
info.app.commit=${GIT_COMMIT:local}

springdoc.api-docs.path=/api/v1/openapi.json
springdoc.swagger-ui.path=/api/v1/docs
springdoc.swagger-ui.operations-sorter=method

# --- API pagination guardrails ---
spring.data.web.pageable.default-page-size=${SPRING_DATA_WEB_PAGEABLE_DEFAULT_PAGE_SIZE:12}
spring.data.web.pageable.max-page-size=${SPRING_DATA_WEB_PAGEABLE_MAX_PAGE_SIZE:100}

# --- Cache / Redis ---
spring.cache.type=${SPRING_CACHE_TYPE:redis}
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.timeout=${SPRING_DATA_REDIS_TIMEOUT:2s}
management.health.redis.enabled=${MANAGEMENT_HEALTH_REDIS_ENABLED:true}
